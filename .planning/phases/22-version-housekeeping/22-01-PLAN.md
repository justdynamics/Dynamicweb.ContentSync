---
phase: 22-version-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj
autonomous: true
requirements:
  - VER-01

must_haves:
  truths:
    - "git tag output shows only 0.x.y tags (0.1.0, 0.1.1, 0.2.0, 0.3.0) with no v-prefixed tags"
    - ".csproj Version reflects current 0.3.0 pre-release version"
  artifacts:
    - path: "src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj"
      provides: "Updated version metadata"
      contains: "<Version>0.3.0"
  key_links:
    - from: "git tag v1.0"
      to: "git tag 0.1.0"
      via: "same commit 5278110"
      pattern: "0\\.1\\.0"
---

<objective>
Re-tag git history from legacy v1.0/v2.0 scheme to 0.x pre-release versioning and update the .csproj to match.

Purpose: Establish consistent semver pre-release versioning across the project. The old v1.0/v2.0 tags implied stable major releases, but this is pre-release software (0.x).
Output: Four new git tags (0.1.0, 0.1.1, 0.2.0, 0.3.0) replacing the four old ones, and .csproj updated to 0.3.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace git tags with 0.x versioning</name>
  <files>n/a (git-only operation)</files>
  <action>
Delete the four legacy tags and recreate them at the same commits with 0.x.y names.

Tag mapping (per VER-01):
- v1.0 (commit 5278110) -> 0.1.0
- v1.1 (commit 26e3bef) -> 0.1.1
- v1.3 (commit a2125e9) -> 0.2.0
- v2.0 (commit 013b9e4) -> 0.3.0

Execute these exact commands in order:

```bash
# Create new tags first (pointing to same commits as old tags)
git tag 0.1.0 v1.0
git tag 0.1.1 v1.1
git tag 0.2.0 v1.3
git tag 0.3.0 v2.0

# Delete old tags
git tag -d v1.0
git tag -d v1.1
git tag -d v1.3
git tag -d v2.0
```

NOTE: These are destructive git tag operations. Creating new tags first ensures we never lose the reference. Tags are local only, no remote push needed.
  </action>
  <verify>
    <automated>git tag | sort -V</automated>
  </verify>
  <done>
git tag output shows exactly: 0.1.0, 0.1.1, 0.2.0, 0.3.0 — no v-prefixed tags remain.
Each new tag points to the same commit as the old tag it replaced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .csproj version to 0.3.0</name>
  <files>src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj</files>
  <action>
Update the version fields in DynamicWeb.Serializer.csproj to reflect current 0.3.0:

- `Version`: change from `0.1.0-beta` to `0.3.0`
- `AssemblyVersion`: change from `0.1.0.0` to `0.3.0.0`

Do NOT change any other fields. The version should be `0.3.0` (not beta, not pre-release suffix) since v0.3.1 milestone is about to complete.
  </action>
  <verify>
    <automated>grep -E "Version|AssemblyVersion" src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj</automated>
  </verify>
  <done>
.csproj shows Version=0.3.0 and AssemblyVersion=0.3.0.0. Build still succeeds (dotnet build produces no errors).
  </done>
</task>

</tasks>

<verification>
1. `git tag | sort -V` outputs exactly: 0.1.0, 0.1.1, 0.2.0, 0.3.0
2. `git log --oneline 0.1.0 -1` matches commit 5278110
3. `git log --oneline 0.3.0 -1` matches commit 013b9e4
4. No v-prefixed tags remain: `git tag | grep "^v"` returns empty
5. `dotnet build src/DynamicWeb.Serializer/DynamicWeb.Serializer.csproj --no-restore` succeeds
</verification>

<success_criteria>
- All four git tags renamed from vX.Y to 0.x.y scheme
- Zero legacy v-prefixed tags remain
- .csproj version updated to 0.3.0
- Project still builds
</success_criteria>

<output>
After completion, create `.planning/phases/22-version-housekeeping/22-01-SUMMARY.md`
</output>
